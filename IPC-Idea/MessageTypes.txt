1. CreateTimer:
	TimerMessage:
		+ isInAppMessage() == True;
		+ getCallback()() --> execute callback immediately
	
ComponentTimer:
	- delete copy/move
	- vTimer
	+ start(milliseconds, callback)
		vTimer->start(reinterpret_cast<unsigned long long>(this), milliseconds, lambda[component]{
			component->onMessageComes(TimerMessage(callback));
		})
		
	+ stop():
		vTimer->stop(reinterpret_cast<unsigned long long>(this))
		
	+ Destructor()
		stop()
	
	
Message:
	+ getType() == AppInternal:
		AppInternalMessage:
			`-> TimerExpire -> execute callback
			`-> MessageID: -> look for correctHandler -> onMessage(Message)
			`-> 
	+ getType() == IPC:
		1. IPCMessage:
			FunctionID: 
			FunctionType:
		
